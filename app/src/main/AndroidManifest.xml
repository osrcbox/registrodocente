<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unl.lapc.registrodocente">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PeriodosActivity"
            android:label="Periodos académicos"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EditPeriodoActivity"
            android:label="Editar periodo"
            android:parentActivityName=".PeriodosActivity" />
        <activity
            android:name=".ClasesActivity"
            android:label="Clases"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EditClaseActivity"
            android:label="Clases"
            android:parentActivityName=".ClasesActivity" />
        <activity
            android:name=".MainClaseActivity"
            android:label="Clase: "
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EditEstudianteActivity"
            android:label="Editar estudiante" />
        <activity android:name=".AsistanciasActivity">
        </activity>
    </application>

</manifest>